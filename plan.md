# Jarvis — architecture & roadmap

Multi-tenant property management. An `Organization` owns `Property` → `Unit` → `Lease`. A `User` joins an org through a `Membership`, which carries an org-scoped `Role`. Tenants are not a separate model — a lease points at the tenant's `Membership`.

## Decision log

| Date | Decision | Why |
|------|----------|-----|
| 2026-08-06 | `start` runs `prisma migrate deploy` before `next start`; a `postinstall` runs `prisma generate` | Found while planning the Railway deploy: `lib/generated/prisma` is gitignored (nothing to import on a clean install) and nothing applied migrations to a fresh deploy target. Verified against a genuinely empty throwaway Postgres container, not the already-migrated dev DB — all 12 migrations applied and every table materialized. `next start` already reads `PORT` from the environment, so Railway needs no extra flag |
| 2026-08-06 | Global search is hand-built on `Dialog` + `Input`, not shadcn's `command` | The registry's `command.tsx` imports `@/app/(create)/components/icon-placeholder` — a path that exists only inside shadcn's repo — and pulls `cmdk`, whose built-in client-side filtering works against server-side search. **Any future `npx shadcn add command` will break the same way.** |
| 2026-08-06 | Client-facing constants live in `lib/search-types.ts`, never imported from `lib/search.ts` | A client component importing a *runtime* value (not just a type) from a Prisma-importing module pulls the driver into the browser bundle — the build fails on `pg`'s `require('dns')`. Types alone are safe (erased); constants are not. Same split as `lib/auth/constants.ts`. **The rule for any new `lib/*` shared with a client component.** |
| 2026-08-05 | **One organization per owner** — enforced in `createOrganizationForUser`, inside the transaction | User decision. A user already holding an Owner membership anywhere gets 409 from `POST /api/organizations`. Inside the `$transaction` so concurrent creates can't both pass the check. Registration needs no separate guard: email/phone are globally unique, so a second signup by the same person 409s before any organization row is created (verified — the rollback leaves nothing behind). Non-owner members are unaffected and can still create one |
| 2026-08-05 | Pending state around a route change uses `useTransition`, never a flag "cleared by unmount" | The org switcher lives in the persistent `(app)` layout, which survives `router.push` + `router.refresh`. A manual `pending` flag left set on the assumption the tree would unmount stayed `true` forever and disabled the switcher after one use. `isPending` is cleared by React when the navigation settles |
| 2026-08-05 | The session's `orgId` is a *claim*, validated against live memberships in `getCurrentUser()` on every request | Trusting it let a removed member keep reading an org's data for the JWT's remaining lifetime (≤7 days) — found while building the org switcher. Validation at the one choke point fixes pages, layout, and every API route together, and replaced the layout's display-only fallback that had let sidebar and page data disagree. Cookies can't be rewritten during render, so the correction is per-request; the cookie catches up at the next switch/login |
| 2026-08-05 | Org switching re-issues the cookie via `POST /api/organizations/switch` and always lands on `/dashboard` | The JWT carries the active org, so switching *is* re-issuing. A new `persist` claim preserves the original remember-me choice across re-issues. Landing on /dashboard because any detail URL from the old org 404s under the new one. Switcher UI lives in the sidebar identity block (`components/org-switcher.tsx`) and only becomes a menu with >1 membership |
| 2026-08-05 | Roles moved to their own `/roles` page; the Users page keeps only invite + member management | Roles were a button and a modal buried on /users, with no way to see what already existed. Permissions will attach to roles, and that needs a surface of its own. The Permissions column ships inert ("Not configured") rather than omitted — it marks the seam without implying enforcement that doesn't exist |
| 2026-08-05 | Owner and Tenant render as **Built-in** roles, not ordinary rows | Both names are matched on in code — the sole-Owner guard in `lib/members.ts`, and every tenant query via `TENANT_ROLE_NAME`. Renaming or deleting either silently breaks those paths, so the UI marks them before any rename/delete affordance exists to be tempted by |
| 2026-08-05 | `TenantStatus` gained a 4th value, **Upcoming**, derived by a single `deriveTenantStatus()` shared by `getTenants` and `getTenantDetail` | Bug found via a real tenant (Jackson Mayunga, lease starting 2 Sept): a signed lease that hasn't started yet fell through the old 3-way branch straight to Vacated, since it wasn't "active" and the tenant "had a lease". Two call sites had each hand-rolled the same logic and had already drifted (one used `hasActive` as a local, the other inlined the `.find`) — collapsing to one function is what makes them impossible to diverge again |
| 2026-08-05 | Dashboard rent: **collected = the whole value of every signed lease** (capped to the months falling in this year), measured against **expected = Σ `Unit.rentAmount` × 12** | User's model: a lease existing *is* the collection event, and the ceiling is what the portfolio would earn fully let, from unit asking prices — not from the leases themselves. Replaced the first pass, which prorated each lease to *today*: on a lease signed two days ago that showed TZS 23,940 out of 900,000, which read as broken. Months, not days, because `leaseAmount = rentAmount * durationMonths` — a month is indivisible here. Still not money *received*; swap in payment rows when a `Payment` model exists |
| 2026-08-05 | Dashboard panels return `PanelList<T> = { items, total }`, never a bare array | The list is capped at `PANEL_ROWS`; the badge must still report the real total. Bundling them means a caller can't reach for `items.length` and under-report. `total` comes from a `prisma.count` sharing the list's hoisted `where` clause, so the two can't describe different sets |
| 2026-08-05 | Dashboard list panels all go through `DashboardPanel` / `PanelRow`, same as the cards go through `MetricCard` | Six panels landed at once (renewals, occupancy, vacancies, move-ins, invites, activity). Without a shared shell that is six copies of the same header, rule, empty state and padding. Any new dashboard list goes through `components/dashboard/panel.tsx` |
| 2026-08-05 | `MetricCard` picks its figure size from the **type** of `value`, not the card's variant | Money is passed pre-formatted (a string) and needs a smaller step than a count (a number) to stay on one line. Keying off `variant` meant the vacancy card's money rendered larger than the rent card's |
| 2026-08-05 | Every dashboard summary card is one `MetricCard` with a `variant`, not a component per look | User call after the first pass shipped two components: the filled card and the count cards then held two independent copies of the same proportions, which is exactly how a card row drifts. Any new dashboard card goes through `components/dashboard/metric-card.tsx` |
| 2026-08-04 | New `--stat` / `--stat-foreground` / `--stat-accent` tokens for the dashboard's filled card | `--primary` is navy in light but **gold** in dark, and `--secondary-foreground` swaps the other way — so a card built on `--primary` turned into a gold slab in dark mode, and the "needs attention" sub-stat read as *less* prominent than the neutral one. The new tokens hold their meaning across themes |
| 2026-08-01 | Prisma 7 with `@prisma/adapter-pg` driver adapter | Prisma 7 requires explicit adapters; client generated to `lib/generated/prisma`, singleton in `lib/prisma.ts` |
| 2026-08-01 | Postgres 16 in Docker, host port 5439 → container 5432 | Avoids clashing with other local Postgres instances |
| 2026-08-01 | `Role` is an org-scoped model, not an enum | Orgs can define custom roles; `@@unique([organizationId, name])`; `Membership.role` is `onDelete: Restrict` so a role in use can't be deleted |
| 2026-08-01 | Ids are `cuid()` | Matches user-provided Lease spec; `HealthCheck` is legacy `uuid()` |
| 2026-08-01 | shadcn on `@base-ui/react`, not Radix | What `components.json` (style `base-nova`) scaffolds; triggers use the `render` prop |
| 2026-08-02 | Custom auth: JWT (`jose`, HS256) + `bcryptjs` | User choice — no auth library/service. `bcryptjs` because native `bcrypt`'s postinstall is blocked by allow-scripts here; `jose` runs in Node and Edge runtimes |
| 2026-08-02 | Phone number is a login identifier, not SMS OTP | No SMS provider in MVP; email stays the required primary identifier, phone optional |
| 2026-08-02 | No email verification / password reset in MVP | Avoids external email provider dependency this sprint |
| 2026-08-02 | Org creation folded into registration (no `/onboarding`) | User decision — registrant names their org at signup and becomes Owner via one `$transaction` |
| 2026-08-02 | Auth flows are route handlers (`/api/auth/*`), pages fetch them | User asked for endpoints; also keeps auth callable by future non-web clients |
| 2026-08-02 | Next 16: middleware is `proxy.ts` at repo root | Confirmed in bundled docs; `proxy.ts` imports only `lib/auth/constants` + `jwt` so Prisma stays out of that bundle |
| 2026-08-02 | App shell: shadcn sidebar, `collapsible="icon"`, route group `app/(app)/` | Icon rail keeps nav reachable when collapsed; sidebar's built-in Sheet handles mobile. Nav defined once in `lib/nav.ts` |
| 2026-08-02 | `hooks/use-mobile.ts` rewritten with `useSyncExternalStore` | Vendored shadcn version violated `react-hooks/set-state-in-effect`; matchMedia is an external store, so this is both lint-clean and tear-free. **Re-applying it via `shadcn add` will overwrite this** |
| 2026-08-02 | Sidebar active nav item uses `bg-primary`/`text-primary-foreground` (not `sidebar-primary`), hover uses `bg-accent`/`text-accent-foreground` (not `sidebar-accent`) | User wants the active state to match real `<Button>` styling app-wide; edited `sidebarMenuButtonVariants` in `components/ui/sidebar.tsx` directly (`sidebar-accent`/`accent` share identical color values in this theme, so hover looks unchanged — only active changed) |
| 2026-08-02 | Property gains `type` (enum), `category`, `ownerName`; `Unit` gains `rentAmount` | Needed by the properties card design (type badge/filter, "Apartments · Kinondoni", owner line, rent roll). Rent is `Int` whole shillings — TSh has no subunit, so this avoids float rounding and Prisma `Decimal` serialization to client components |
| 2026-08-02 | Occupancy = units with an *active* lease (`startDate <= now` and `endDate` null or future); rent roll counts occupied units only | Rent roll should reflect what is actually billed, so vacant units contribute nothing; the detail page shows potential and vacancy loss separately |
| 2026-08-02 | `Property.ownerName` dropped — owner is derived from the org's Owner-role member | User decision. Note the trade-off they accepted: every property in an org now shows the same owner, so a building belonging to a different landlord can't be recorded. Resolved in `lib/organizations.ts`, cached per render, falling back to the organization name if the Owner role is renamed or removed |
| 2026-08-02 | Properties CRUD lives at `/api/properties`; pages still call `lib/properties.ts` directly | One data-access layer, two consumers — no page pays an HTTP round trip to its own API. `requireActiveOrg()` in `lib/api-auth.ts` gives every route the same 401/403 handling |
| 2026-08-02 | `prisma/seed.ts` deleted; amenity/category options are code constants in `lib/property-options.ts` | With no lookup tables there is no reference data to seed. Options are served by `GET /api/properties/options` and must stay in step with the icon switch in `components/properties/property-icon.tsx` |
| 2026-08-03 | `User.passwordHash` is nullable; null means "cannot sign in" | Assisted-onboarding tenants and unaccepted invitees have no password. **Login must reject null explicitly** (it does, with the same generic 401 + dummy hash so the account isn't disclosed). Replaced the old `"!seeded-no-login"` sentinel, which wrongly reported `canSignIn: true` |
| 2026-08-03 | `User.email` is nullable; tenant onboarding requires **phone** instead | Many tenants have a phone but no email. Phone stays nullable in the DB — 11 existing users have none, so a NOT NULL would have meant inventing numbers — and "phone required" is enforced in the tenant form/schema only |
| 2026-08-03 | Invitations are shareable links: random 32-byte token, only its SHA-256 stored | No email provider. SHA-256 rather than bcrypt because the token is already high-entropy and lookup must be deterministic; a DB leak yields no usable links. Single-use, 14-day expiry, `/invite/[token]` is public in `proxy.ts` |
| 2026-08-03 | Tenant status is derived, never stored: Active / Vacated / Prospect | Derived from leases so it can't drift. Active = current lease, Vacated = lease ended, Prospect = never had one |
| 2026-08-03 | **Lease can cross organizations — the schema does not prevent it** | Found live: three leases joined a Madadi membership to a CpMall unit, leaking another org's member names onto the property page. `activeLeaseFilter` now constrains `membership.organizationId`. A DB-level constraint would be the real fix |
| 2026-08-03 | Currency is `TZS`, defined once as `CURRENCY` in `lib/format.ts` with `formatCurrency`/`formatCurrencyFull` | TZS is the ISO 4217 code; it was previously the literal "TSh" hardcoded at each call site |
| 2026-08-03 | `Unit` gains `minTenureMonths`, `unitType`, `floor`, `block`, `sizeSqm`, `amenities` — **all optional** | Existing units predate them, so requiring any would mean inventing values. `floor` is `String?` because "Ground"/"Mezzanine"/"Basement" are real; `sizeSqm` is `Float?` for 62.5 m²; `unitType` is named to avoid colliding with `Property.type` (RESIDENTIAL/COMMERCIAL) |
| 2026-08-03 | Unit amenities are separate from property amenities | A unit has "Furnished", "Built-in wardrobes"; the building has "Lift", "Generator". Two option lists: `lib/unit-options.ts` and `lib/property-options.ts` |
| 2026-08-03 | Checkbox border uses `border-ring`, not `border-input` | `--input` is `oklch(0.97…)` in light mode — effectively white, so unchecked boxes were invisible on a card |
| 2026-08-03 | Dialog forms reset via a `key` remount, never a `useEffect` | `react-hooks/set-state-in-effect` forbids the effect-reset pattern; keying on `${id}:${open}` re-seeds state from props on each open |
| 2026-08-02 | `Property` gains `status` (enum), `description`, `amenities String[]` | Required by the property-detail design (Property details card, Description, General facility amenities) |
| 2026-08-02 | Units tab uses TanStack Table via `components/ui/data-table.tsx` | shadcn ships no `data-table` registry item — it's a recipe over `@tanstack/react-table`. Generic wrapper gives sorting, filtering, faceted filter, row selection and pagination to any future table (tenants, leases) |
| 2026-08-02 | **Regenerating the Prisma client requires restarting the dev server** | Turbopack caches the old client, so new fields read as `undefined` at runtime even though `tsc` passes. Fix: stop server, `rm -rf .next/dev`, restart. Hit this twice |
| 2026-08-02 | Base UI `SelectValue` needs a function child to show labels | It renders the raw value otherwise (the status filter displayed "all" instead of "All statuses") |
| 2026-08-02 | Base UI `Checkbox` takes `indeterminate` as its own prop | Unlike Radix, where indeterminate is a `checked` value — matters for the table's select-all header |
| 2026-08-02 | `Button` rendering a `<Link>` needs `nativeButton={false}` | Base UI errors at runtime ("expected a native \<button\>") when `render` swaps in an anchor. **Applies to every future `Button render={<Link/>}`** |
| 2026-08-02 | Icons resolved via a `PropertyIcon` component returning JSX per branch, not a function returning a component | `react-hooks/static-components` flags a helper that returns a component type as "created during render" |
| 2026-08-02 | `DropdownMenuLabel` must be wrapped in `DropdownMenuGroup` | Found via testing: base-ui's `Menu.GroupLabel` (what shadcn's `DropdownMenuLabel` renders) throws "MenuGroupContext is missing" without a `Menu.Group` ancestor — unlike Radix, where `Label` is standalone. Fixed in `components/nav-user.tsx`. **Any future `DropdownMenuLabel` usage in this codebase must be wrapped in `DropdownMenuGroup`** |

## Auth design (current sprint)

- **Credentials**: email (required, unique) or phone (optional, unique) + password. Hash with `bcryptjs` (cost 10). Zod validation, password min length 8.
- **Session**: JWT signed with `jose` HS256, secret in `AUTH_SECRET` env. Stored in httpOnly cookie: `sameSite=lax`, `secure` in prod, 7-day expiry. Payload `{ sub: userId, orgId: activeOrgId | null }`.
- **Helpers** (`lib/auth/`): `hash.ts`, `jwt.ts`, `session.ts` (`getCurrentUser()` / `requireUser()` wrapped in React `cache()`), zod schemas.
- **Flows** (route handlers): `POST /api/auth/register`, `POST /api/auth/login` (identifier resolves as email if it contains `@`, else phone), `POST /api/auth/logout`, `GET /api/auth/me`.
- **Route protection**: `proxy.ts` at repo root (Next 16's renamed middleware) redirects unauthenticated page requests to `/login` and bounces authed users off `/login`/`/register`; API routes enforce auth themselves.
- **Org creation**: happens inside registration — one `$transaction` creates `User` + `Organization` + seeds an `Owner` `Role` + `Membership`, then the session cookie is issued with that `orgId`. Org switcher (re-issue cookie with new `orgId`) deferred until invitations exist.
- **Schema delta** (`auth_fields` migration): `User.passwordHash String`, `User.phone String? @unique`.

## Out of scope (future sprints)

SMS OTP (needs Twilio or similar) · email verification + password reset (needs Resend or similar) · member invitations · rate limiting / login lockout · RBAC permission enforcement beyond role seeding · refresh-token rotation.
